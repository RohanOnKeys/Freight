version: 1

jobs:
  build:
    stage: build
    image: python:3.12

    script:
      - echo "Freight artifact upload test" > test.txt
      - echo "Runner executed successfully" >> test.txt

    artifacts:
      paths:
        - test.txt